# 📩 Email → WhatsApp Notification System

Automatically receive WhatsApp notifications whenever you get a new email in your inbox.

This project uses:
- **Python**
- **IMAP** (Gmail email reading)
- **Meta WhatsApp Cloud API**
- **dotenv (.env) for secure credentials**

---

## 🚀 Features

- Reads **unread emails** using IMAP
- Sends WhatsApp notification for:
  - Sender
  - Subject
  - Email snippet (first 200 characters)
- Uses **official WhatsApp Cloud API**
- Fully secure with `.env`
- Can run automatically every minute (cron job / cloud)
- Easy to deploy (Render / Railway / PythonAnywhere)

---

## 📁 Project Structure

